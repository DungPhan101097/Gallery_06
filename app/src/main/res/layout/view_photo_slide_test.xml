<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".presentation.uis.activities.PreviewPhotoActivity">

    <include layout="@layout/view_photo_slide" />


    <android.support.v7.widget.Toolbar
        android:id="@+id/photo_toolbar_top"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:layout_marginTop="0dp"
        app:title="Photo"
        android:background="@color/white"
        app:titleTextColor="@color/black"
        />

    <TextView
        android:id="@+id/txtDes"
        android:layout_width="match_parent"
        android:layout_height="93dp"
        android:layout_gravity="bottom"
        android:background="#5c7a7a7a"
        android:text="Chú thích ảnh"
        android:textColor="@android:color/black"
        tools:layout_editor_absoluteY="474dp" />

    <android.support.design.widget.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginBottom="0dp"
        app:itemBackground="@color/white"
        app:menu="@menu/photo_bottom_menu"
        app:itemTextColor="@color/black"
        app:itemIconTint="@color/black"
        />

</android.support.design.widget.CoordinatorLayout>
